apiVersion: v1
kind: ResourceQuota
metadata:
  name: platform-prod-quota
  namespace: platform-prod
spec:
  hard:
    pods: "10"
    requests.cpu: "2"
    requests.memory: 4Gi
    limits.cpu: "4"
    limits.memory: 8Gi
    services: "5"
    persistentvolumeclaims: "3"