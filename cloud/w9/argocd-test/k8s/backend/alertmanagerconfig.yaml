apiVersion: monitoring.coreos.com/v1alpha1
kind: AlertmanagerConfig
metadata:
  name: backend-email-alerts
  namespace: argocd-test
  labels:
    release: monitoring
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  route:
    receiver: backend-email
    groupBy:
      - alertname
      - namespace
      - service
    groupWait: 10s
    groupInterval: 30s
    repeatInterval: 1h
  receivers:
    - name: backend-email
      emailConfigs:
        - to: "lenguyennhatthanh72@gmail.com"
          from: "lenguyennhatthanh72@gmail.com"
          smarthost: "smtp.gmail.com:587"
          authUsername: "lenguyennhatthanh72@gmail.com"
          authIdentity: "lenguyennhatthanh72@gmail.com"
          authPassword:
            name: alertmanager-email-secret
            key: smtp-password
          requireTLS: true
          sendResolved: true
